import { DisableElementDirective } from './disable-element.directive';

describe('DisableElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableElementDirective();
    expect(directive).toBeTruthy();
  });
});
